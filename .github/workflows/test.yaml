name: test duplicate

on: 
  push:
    branches:
    - '**'
  pull_request:
    types:
    - synchronize
    - opened
    - reopened

env:
  GITHUB_REPOSITORY: Cicatrice/apodex

jobs:
  go:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo ${{ toJSON(on) }}
        echo ${{ toJSON(jobs) }}
        echo ${{ toJSON(steps) }}

